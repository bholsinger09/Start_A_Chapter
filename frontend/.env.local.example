# Local environment variables (only for development)
# Remove these lines or comment them out for production builds
# VUE_APP_API_BASE_URL=http://localhost:8080
# VITE_API_BASE_URL=http://localhost:8080

# Note: .env.local overrides other environment files
# For production deployment, these should be commented out
# so that .env.production takes precedence

# Node Environment
NODE_ENV=development